{% extends 'base.html.twig' %}

{% block title %}Ajouter une session{% endblock %}

{% block body %}



    {# {{ form(formAddEmploye) }} #}

    {% if edit %}
    <h1>Editer une session</h1>
    {% else %}
    <h1>Ajouter une session</h1>
    {% endif %}


    {{ form_start(formAddSession) }}

        <div>
            {# <div {{ form_row(formAddSession.formation.name) }}</div> #}
            {# <div {{ form_row(formAddStudent.firstName) }}</div> #}
        </div>

        <div>
            {# <div {{ form_row(formAddSession.nbPlaceTotal) }}</div> #}
        </div>

        
        {{ form_row(formAddSession.Validate) }}
        

    {{ form_end(formAddSession) }}


{% endblock %}